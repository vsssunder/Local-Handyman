# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure
runConfig:
  minInstances: 0
  maxInstances: 100
  cpu: 1
  memoryMiB: 1024
# In case of a build failure, specify a longer timeout.
# timeoutSeconds: 3600
# In case of a build failure, you can specify a supported Node.js version.
# nodeVersion: 20
